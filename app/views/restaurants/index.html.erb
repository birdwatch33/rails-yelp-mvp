<div class="container">
  <h1>All Restaurants</h1>
  <% @restaurants.each do |restaurant| %>
  <div class="row">
    <div class="column"><%= link_to restaurant.name, restaurant_path(restaurant) %></div>
  </div>
  <% end %>
  <%= link_to "Add Restaurant", new_restaurant_path, class: "btn btn-primary"%>

</div>
